# PLAN.md

This is my personal working file for tracking goals, tasks, and next steps.
Update it freely — it's not documentation, it's a scratchpad with structure.

---

## Current focus

> What am I working on right now?

<!-- Replace this with your current focus, e.g.:
     "Getting comfortable with GitHub Codespaces and the terminal"
-->

---

## Goals

### Short-term (this week)
- [ ] Open this repo in a Codespace and verify all tools are working
- [ ] Run `make doctor` and confirm output
- [ ] Try `fzf`, `rg`, and `eza` in the terminal
- [ ] Make a small change and commit it using the `gh` CLI

### Medium-term (this month)
- [ ] Build a small project inside this workspace (Node or Python)
- [ ] Get comfortable with GitHub Copilot suggestions
- [ ] Learn basic `git` branching and pull requests
- [ ] Explore `pnpm` workspaces if managing multiple packages

### Longer-term
- [ ] Use this workspace as the base for a real project
- [ ] Customise the devcontainer further as my needs grow
- [ ] Explore GitHub Actions for automation

---

## Completed

<!-- Move finished items here with a date -->

---

## Notes & ideas

<!-- Anything I don't want to forget: links, thoughts, half-formed ideas -->

- VS Code shortcut to open the terminal: `` Ctrl+` ``
- Run `gh auth login` once inside the Codespace to authenticate with GitHub
- Use `bat filename` for syntax-highlighted file viewing (don't alias over `cat` — it can break scripts)
- Use `fd` instead of `find`: e.g. `fd ".json"` to find all JSON files (don't alias over `find` — syntax differs)
